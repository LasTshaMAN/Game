{
  "name": "game",
  "version": "0.0.1",
  "scripts": {
    "clean": "cleandir ./dist",
    "compile": "npm run clean && tsc",
    "test": "mocha --compilers ts:ts-node/register ./test/**/*",
    "build": "npm run test && webpack"
  },
  "dependencies": {
    "typescript": "^2.1.4",
    "webpack": "^1.14.0"
  },
  "devDependencies": {
    "@types/chai": "^3.4.34",
    "@types/mocha": "^2.2.34",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "ts-node": "^1.7.2"
  }
}
